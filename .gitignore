# Python / uv
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Evidence & timeline artifacts — never commit event logs or timelines
*.evtx
*.evtx.zip
timelines/
output/
*.csv
*.jsonl

# Derived rule index
.cache/

# Downloaded STIX bundles — ~50MB each, regenerate with the scripts that use them
*.stix.json
enterprise-attack*.json
stix-atlas*.json
